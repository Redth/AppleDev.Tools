name: Run iOS Simulator

on:
  pull_request:
  push:
    branches: [ main ]

jobs:
  run:
    name: Run
    runs-on: macos-latest
    steps:
    - name: 📥 Checkout
      uses: actions/checkout@v5
      with:
        fetch-depth: 0
    - name: 🛠️ Setup Xcode
      uses: maxim-lobanov/setup-xcode@v1
      with:
        xcode-version: '26.2'
    - name: ⚙️ Setup .NET
      uses: actions/setup-dotnet@v5
      with:
        dotnet-version: 10.0.x
    - name: 📦 Install iOS workload
      run: dotnet workload install ios --source https://api.nuget.org/v3/index.json
    - name: 🔨 Build the project
      run: GITHUB_ENV=/dev/null dotnet build AppleDev.Tool/AppleDev.Tool.csproj

    - name: 📋 List available simulators
      run: dotnet run --no-launch-profile --project AppleDev.Tool -- simulator list

    - name: 🆕 Create and boot simulator
      id: create-sim
      run: |
        RESULT=$(dotnet run --no-launch-profile --project AppleDev.Tool -- simulator create "E2ETestSim" --device-type "iPhone 16" --boot --wait --format json)
        echo "Simulator create result: $RESULT"
        UDID=$(echo "$RESULT" | python3 -c "import sys,json; print(json.load(sys.stdin)['udid'])" 2>/dev/null || echo "")
        echo "UDID: $UDID"
        echo "sim_udid=$UDID" >> $GITHUB_OUTPUT

    - name: ✅ Verify simulator is booted
      run: xcrun simctl list devices | grep "E2ETestSim" | grep "Booted" && echo "✅ Simulator is booted" || echo "⚠️ Simulator may not be booted"

    - name: 📱 Create test iOS app
      run: |
        dotnet new ios -n TestApp --output TestApp
        # Add a log marker inside FinishedLaunching to prove the app runs real code
        sed -i.bak 's/return true;/System.Console.WriteLine("TestApp: E2E_APP_STARTED_SUCCESSFULLY"); return true;/' TestApp/AppDelegate.cs
        dotnet build TestApp/TestApp.csproj -f net10.0-ios -c Release -r iossimulator-arm64

    - name: 📦 Install app on simulator
      run: |
        APP_PATH=$(find TestApp -name "TestApp.app" -path "*/bin/*" | head -1)
        echo "Installing app: $APP_PATH"
        dotnet run --no-launch-profile --project AppleDev.Tool -- simulator app install "E2ETestSim" --path "$APP_PATH"

    - name: 🚀 Launch test app
      run: dotnet run --no-launch-profile --project AppleDev.Tool -- simulator app launch "E2ETestSim" --bundle-id com.companyname.testapp

    - name: 📋 Capture and verify logs
      run: |
        sleep 5
        dotnet run --no-launch-profile --project AppleDev.Tool -- simulator logs "E2ETestSim" --format text > simulator-logs.txt 2>&1 || true
        echo "--- Checking for app log marker ---"
        grep "E2E_APP_STARTED_SUCCESSFULLY" simulator-logs.txt && echo "✅ App ran successfully" || echo "⚠️ App log marker not found (may need predicate filter)"

    - name: 🧹 Cleanup simulator
      if: always()
      run: dotnet run --no-launch-profile --project AppleDev.Tool -- simulator delete --force "E2ETestSim"

    - name: 📤 Upload logs
      if: always()
      uses: actions/upload-artifact@v5
      with:
        name: simulator-logs
        path: simulator-logs.txt
        if-no-files-found: ignore
